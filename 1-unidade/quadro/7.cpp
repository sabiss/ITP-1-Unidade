#include <bits/stdc++.h>

using namespace std;

int main(){
    int n = 0;
    cin >>n;

    int contador[n+1];

    for(int i = 1;i<n+1;i++){
        int valor = 0;
        contador[valor]++;
    }
    pair<int, int> maisFrequente;//numero, frequencia

    for(int i = 1; i<n+1; i++){
        if(contador[i]>maisFrequente.second)
    }
}